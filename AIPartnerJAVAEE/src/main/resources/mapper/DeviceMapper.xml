<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aipartner.mapper.DeviceMapper">

    <insert id="insert">
        insert into db_aipartner.device(dev_id, userid, password)
        values(#{devId}, #{userid}, #{password})
    </insert>

    <select id="findById" resultType="com.example.aipartner.pojo.device.Device">
        select * from db_aipartner.device where dev_id = #{devId}
    </select>
    <select id="findDevId" resultType="java.lang.String">
        select dev_id from db_aipartner.device where userid = #{userid} and password = #{password}
    </select>

    <select id="listByUserId" resultType="com.example.aipartner.pojo.device.Device">
        select * from db_aipartner.device where userid = #{userId}
    </select>

    <select id="listInfoByUserId" resultType="com.example.aipartner.pojo.device.DeviceInfo">
        select d.dev_id,
               d.userid,
               i.id,
               i.img_url,
               i.device_name_user,
               i.device_name_orgin
        from db_aipartner.device d
        left join db_aipartner.device_imgandname i on i.device_id = d.dev_id
        where d.userid = #{userId}
    </select>

    <update id="updateBinding">
        update db_aipartner.device
        set userid = #{userid},
            password = #{password}
        where dev_id = #{devId}
    </update>

    <update id="updateDeviceNameUser">
        update db_aipartner.device_imgandname
        set device_name_user = #{deviceNameUser}
        where device_id = #{deviceId}
    </update>

</mapper>